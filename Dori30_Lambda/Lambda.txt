람다식
간단하고 재활용이 필요없는 함수를 만들 때 사용
함수 및 변수의 이름을 고민할 시간을 줄일 수 있음
별도로 함수를 추가하지 않고 함수 안에서 호출하여 사용
무명(익명) 메서드에서 발전된 형태 (무명 메서드에서 선언 부분을 삭제)
함수의 인자 값이 메서드 형태일 경우 주로 사용된다.

C# 에서는 delegate 무명 메서드를 Generic 형태로 정의해 놓는다.
Func<T, T 반환형> : 반환형이 있는 delegate Type
Action<T> : 반환형이 없는 delegate Type

(입력 파라미터) => { 실행 문장 블럭 };